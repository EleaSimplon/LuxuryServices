{% extends 'base.html.twig' %}

{% block body %}

    <!-- Page Header-->
    <section class="page-title page-index-admin fixed-bg overlay dark-5 padding-top-160 padding-bottom-80">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h2 class="white-text">Add an offer</h2>
                    <span class="white-text">New offer</span>
                    <ol class="breadcrumb">
                        <li><a href="{{ path ('home') }}">Home</a></li>
                        <li class="active">New Offer</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>

    {{ include('job_offer/_form.html.twig') }}

    <a href="{{ path('offerList') }}">back to list</a>
{% endblock %}
