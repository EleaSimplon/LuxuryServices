{% extends 'base.html.twig' %}

{% block body %}

    <section class="page-title page-index-admin fixed-bg overlay dark-5 padding-top-160 padding-bottom-80">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h2 class="white-text">{{ user.firstName}} {{ user.lastName}}</h2>
                    <span class="white-text">Add your comment</span>
                    <ol class="breadcrumb">
                        <li><a href="{{ path ('home') }}">Home</a></li>
                        <li class="active">Show User List</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>

    
    <div class="row center">
        <div class="col-xs-12 col-sm-12 col-md-12">
            {{ include('info_admin_candidat/_form.html.twig') }}
        </div>
    </div>   
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12">
            <a class="btn red lighten-2" href="{{ path('admin_user_show', {'id' : user.id}) }}"><i class="material-icons prefix">arrow_back</i> back to user {{ user.firstName}} </a>
        </div>
    </div>   

{% endblock %}
